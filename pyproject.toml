[build-system]
requires = ["flit_core >=3.2,<4", "pydata-sphinx-theme >= 0.13.0"]
build-backend = "flit_core.buildapi"

[project]
name = "lumache"
authors = [{name = "Graziella", email = "graziella@lumache"}]
dynamic = ["version", "description"]
